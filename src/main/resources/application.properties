# Spring Boot Application Configuration

# Server port
server.port=8080

# Context path (matches original Jetty configuration)
server.servlet.context-path=/opensaml5-webprofile-demo

# Server address (bind to all interfaces for dev container compatibility)
server.address=0.0.0.0

# Logging
logging.level.root=INFO
logging.level.no.steras.opensamlSamples=INFO
